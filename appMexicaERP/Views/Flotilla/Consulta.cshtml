
@{
    ViewBag.Title = "Consulta";
}

<div class="container">
    <!-- begin row -->
    <div class="row">
        <div class="col-md-12">
            <!-- begin panel -->
            <div class="panel panel-inverse">
                <div class="panel-heading">
                    <h4 class="panel-title">Listado de Vehiculos :: Mexica Tours</h4>
                </div>
                <div class="alert alert-warning fade in">
                    <button type="button" class="close" data-dismiss="alert">
                        <span aria-hidden="true">&times;</span>
                    </button>
                    Catalogo de unidades de MexicaTours, este propociona la información de las unidades, como compañia de seguros, vigencia, kilometraje del vehiculos y más, asi como el operador que esta a cargo de la misma.
                </div>
                <div class="panel-body">
                    <table id="data-table" class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>N° Economico</th>
                                <th>Placas</th>
                                <th>CIA Aseguradora</th>
                                <th>Inicia Seguro</th>
                                <th>Finaliza Seguro</th>
                                <th>Empresa</th>
                                @*<th>Operador</th>*@
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in ViewBag.listaFlotilla)
                            {
                                <tr class="odd gradeX">

                                    <td><a href="#" onclick="modificar('@item.idFlotilla')">@item.idFlotilla </a></td>
                                    <td>@item.numEconomico</td>
                                    <td>@item.placas</td>
                                    <td>@item.ciaSeguro</td>
                                    <td>@item.vigenciaIniciaSeguro</td>
                                    <td>@item.vigenciaFinSeguro</td>
                                    <td>@item.idEmpresa</td>
                                    @*<td>@item.idEmpleado</td>*@
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<script>


    function modificar(idFlotilla)
    {
            //return window.location.href='{{url("hotel/modificar")}}/'+id_hotel;
        window.location.href = '@Url.Action("Modificar", "Flotilla")' + '/' + idFlotilla;
    }
</script>